{
  "name": "votedisha-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "functions",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix functions",
    "build:all": "npm run build --prefix functions && npm run build --prefix frontend",
    "lint:all": "npm run lint --prefix functions && npm run lint --prefix frontend",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.9",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
